{
  "mongo": {
    "uri": ${?MONGO_URI}
    "db": "test",
    "collection": "docker-test",
    "options": {
      "directConnection": true
    }

  },
  "kafka": {
    "brokers": [${?KAFKA_URI}],
    "clientId": "docker-mongo-event-builder-test",
    "topic": "docker-test"
  }
}