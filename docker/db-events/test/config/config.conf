{
  "mongo": {
    "uri": ${?MONGO_URI}
    "db": "test",
    "collection": "docker-test",
    "options": {
      "directConnection": true
    }

  },
  "kafka": {
    "brokers": [${?KAFKA_URI}],
    "host": "localhost",
    "clientId": "mongo-event-builder-test",
    "topic": "docker-test"
  }
}