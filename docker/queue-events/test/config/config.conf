{
  kafka: {
    brokers: [
      ${?KAFKA_URI},
    ],
    host: "localhost",
    clientId: "kafka-event-consumer-test",
    topic: ${topic},
    groupId: ${topic}
  },
  schema: "test.schema.json",
  swagger: "test.swagger.json",
}