{
      kafka: {
        brokers: [${?KAFKA_URI}],
        host: ${?KAFKA_HOST},
        clientId: "kafka-event-consumer-test",
        topic: "test",
        groupId: "QE-Test"
      },
      schema: "config/test.schema.json",
      swagger: "config/test.swagger.json",
    }